#!/usr/bin/env node
import 'source-map-support/register';
export declare const config: {
    repositoryName: string;
    branch: string;
    runtime: {
        nodejs: number;
    };
    cdkVersion: string;
};
